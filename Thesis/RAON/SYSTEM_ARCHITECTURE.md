# System Architecture Diagram

## High-Level Overview

```
┌────────────────────────────────────────────────────────────────────┐
│                      RAON VENDING MACHINE                           │
│                      (Raspberry Pi 4)                               │
└────────────────────────────────────────────────────────────────────┘

┌────────────────────────────────────────────────────────────────────┐
│                         User Interface                              │
│                      (tkinter GUI - Main.py)                        │
│  ┌──────────────────────────────────────────────────────────────┐  │
│  │                   Selection Screen / Kiosk                    │  │
│  │  ┌─────────────────────────────────────────────────────────┐ │  │
│  │  │              Product Grid                               │ │  │
│  │  │              (Item Selection)                           │ │  │
│  │  └─────────────────────────────────────────────────────────┘ │  │
│  │  ┌─────────────────────────────────────────────────────────┐ │  │
│  │  │  🔧 SYSTEM STATUS PANEL (NEW)                          │ │  │
│  │  │  ┌─────────────────────────────────────────────────────┤ │  │
│  │  │  │ 🌡️ ENV │ ❄️ TEC │ 📡 IR │ ⚙️ HEALTH              │ │  │
│  │  │  │ Real-time sensor & hardware status display         │ │  │
│  │  │  └─────────────────────────────────────────────────────┤ │  │
│  │  │  (Updates every 1 second)                              │ │  │
│  │  └─────────────────────────────────────────────────────────┘ │  │
│  └──────────────────────────────────────────────────────────────┘  │
└────────────────────────────────────────────────────────────────────┘

┌────────────────────────────────────────────────────────────────────┐
│                    Hardware Controllers                              │
├────────────────────────────────────────────────────────────────────┤
│                                                                      │
│  ┌─────────────────────────┐  ┌──────────────────────────────────┐ │
│  │   DHT22 Sensors         │  │   TEC Controller                │ │
│  │  (GPIO27, GPIO22)       │  │   (GPIO26 relay)               │ │
│  │                         │  │                                │ │
│  │  • Temp readings        │  │  • Monitors DHT22 temps        │ │
│  │  • Humidity readings    │  │  • Hysteresis control          │ │
│  │  • 2-second interval    │  │  • Turns cooling ON/OFF        │ │
│  │                         │  │  • Provides status updates     │ │
│  └────────┬────────────────┘  └─────────────┬──────────────────┘ │
│           │                                  │                     │
│           │                ┌─────────────────┘                     │
│           │                │                                       │
│           └────────┬───────┴──────────┐                            │
│                    │                  │                            │
│  ┌─────────────────────────────────────────────────────────────┐ │
│  │           Status Callbacks to main.py                       │ │
│  │  ┌────────────────────────────────────────────────────────┐ │ │
│  │  │ _on_tec_status_update(enabled, active, target, temp)  │ │ │
│  │  │ _on_ir_status_update(s1, s2, mode, last_detection)    │ │ │
│  │  │ _on_dht22_update(sensor#, temp, humidity)             │ │ │
│  │  └────────────────────────────────────────────────────────┘ │ │
│  └──────────────┬──────────────────────────────────────────────┘ │
│                 │                                                  │
│  ┌──────────────────────────────────────────────────────────────┐ │
│  │         IR Sensor Monitor                                    │ │
│  │         (GPIO6, GPIO5)                                        │ │
│  │                                                              │ │
│  │  • Detects items in bin                                    │ │
│  │  • Multi-sensor detection logic                           │ │
│  │  • Provides IR status updates                             │ │
│  │  • Timeout alerts                                         │ │
│  └──────────────┬───────────────────────────────────────────────┘ │
│                 │                                                  │
└─────────────────┼──────────────────────────────────────────────────┘
                  │
                  │ Callbacks
                  ↓
┌────────────────────────────────────────────────────────────────────┐
│                    Main Application (main.py)                       │
│  ┌──────────────────────────────────────────────────────────────┐  │
│  │ Callback Handlers:                                           │  │
│  │ • _on_tec_status_update()    → status_panel.update_tec()   │  │
│  │ • _on_dht22_update()         → status_panel.update_dht22() │  │
│  │ • _on_ir_status_update()     → status_panel.update_ir()    │  │
│  │                                                              │  │
│  │ Integration Layer:                                           │  │
│  │ • Registers callbacks with hardware controllers             │  │
│  │ • Routes status updates to UI panel                         │  │
│  │ • Manages initialization and cleanup                        │  │
│  └──────────────────────────────────────────────────────────────┘  │
└────────────────────────────────────────────────────────────────────┘

┌────────────────────────────────────────────────────────────────────┐
│                  System Status Panel (NEW)                          │
│  ┌──────────────────────────────────────────────────────────────┐  │
│  │ Internal State:                                              │  │
│  │ • dht22_data: {sensor_1: {temp, humidity}, ...}            │  │
│  │ • tec_status: {enabled, active, target, current}           │  │
│  │ • ir_status: {sensor_1, sensor_2, mode}                    │  │
│  │ • system_health: operational / warning / error              │  │
│  │                                                              │  │
│  │ Background Thread:                                          │  │
│  │ • Updates display every 1 second                            │  │
│  │ • Non-blocking (daemon thread)                              │  │
│  │ • Thread-safe (uses locks)                                  │  │
│  │                                                              │  │
│  │ Display Sections:                                           │  │
│  │ ┌─────────────┬─────────────┬──────────┬──────────────┐    │  │
│  │ │ 🌡️ ENV     │ ❄️ TEC      │ 📡 IR    │ ⚙️ HEALTH  │    │  │
│  │ │ S1/S2 readings│ Status   │ Sensors  │ Overall    │    │  │
│  │ └─────────────┴─────────────┴──────────┴──────────────┘    │  │
│  │                                                              │  │
│  │ Color Coding:                                               │  │
│  │ 🟢 Green   - Operational / Active / Ready                  │  │
│  │ 🟠 Orange  - Item Present / Warning                        │  │
│  │ 🔴 Red     - Error / Absent / Off                          │  │
│  │ ⚪ Gray    - Disabled / Unknown                            │  │
│  └──────────────────────────────────────────────────────────────┘  │
└────────────────────────────────────────────────────────────────────┘
```

## Data Flow Diagram

```
┌─────────────────┐
│ DHT22 Sensors   │
│  (GPIO27, 22)   │
└────────┬────────┘
         │ Read temp/humidity
         │ every 2 seconds
         ↓
    ┌─────────────────────────┐
    │ dht11_handler.py         │
    │ DHT22Sensor class       │
    └────────┬────────────────┘
             │ Temperature data
             ↓
┌──────────────────────────┐
│ tec_controller.py        │
│ TECController class      │ ←─┐ Monitors temp
│                          │   │ Hysteresis control
│ _control_loop()          │   │ GPIO26 relay
└────────┬─────────────────┘   │
         │                      │
         │ Status callback     │
         │ every 2-3 sec       │
         ↓                      │
    ┌────────────────────────────────────┐
    │ main.py                            │
    │ _on_tec_status_update()            │
    └────────┬──────────────────────────┘
             │
             │ Display update
             ↓
    ┌────────────────────────────────────┐
    │ system_status_panel.py             │
    │ update_tec_status()                │
    │ → Updates TEC display section      │
    └────────┬──────────────────────────┘
             │
             ↓
    ┌────────────────────────────────────┐
    │  UI Kiosk Frame                    │
    │  Status Panel Widget Display       │
    │  ❄️ TEC: ON, Target: 10°C, ...    │
    └────────────────────────────────────┘


┌────────────────────────────┐
│ IR Sensors                 │
│ (GPIO6, GPIO5 in bin)      │
└────────┬───────────────────┘
         │ Read GPIO high/low
         │ 500ms poll
         ↓
┌────────────────────────────────────┐
│ item_dispense_monitor.py           │
│ ItemDispenseMonitor class          │
│                                    │
│ _monitor_loop()                    │
│ → Reads all sensors                │
│ → Detection logic (any/all/first)  │
│ → Calls IR status callback         │
└────────┬───────────────────────────┘
         │
         │ IR status callback
         │ every 500ms
         ↓
┌────────────────────────────────────┐
│ main.py                            │
│ _on_ir_status_update()             │
└────────┬───────────────────────────┘
         │
         │ Display update
         ↓
┌────────────────────────────────────┐
│ system_status_panel.py             │
│ update_ir_status()                 │
│ → Updates IR display section       │
└────────┬───────────────────────────┘
         │
         ↓
┌────────────────────────────────────┐
│  UI Kiosk Frame                    │
│  Status Panel Widget Display       │
│  📡 IR: S1=EMPTY, S2=PRESENT       │
└────────────────────────────────────┘
```

## Thread Architecture

```
Main Thread (GUI)
├─ Tkinter event loop (responsive)
├─ frame.pack() operations
├─ Button/Label updates
└─ System Status Panel (in main thread)

    ↓ Creates background threads

Background Thread 1: TEC Controller
├─ Runs continuously
├─ Reads DHT22 sensors every 2 seconds
├─ Calculates control temperature
├─ Calls status callback
└─ Controls GPIO26 relay

Background Thread 2: IR Sensor Monitor
├─ Runs continuously
├─ Polls IR sensors every 500ms
├─ Detects item dispensing
├─ Calls IR status callback
└─ Monitors dispense timeouts

Background Thread 3: Status Panel Updater
├─ Updates display every 1 second
├─ Reads thread-safe data (locked)
├─ Updates label texts
└─ Non-blocking (daemon)

All threads are daemon threads:
└─ Automatically stop when main app exits
```

## GPIO Pin Mapping

```
┌──────────────────────────────────────────────────────────┐
│              Raspberry Pi 4 GPIO Pins                     │
├──────────────────────────────────────────────────────────┤
│                                                           │
│  GPIO17  ← Coin Acceptor (binary detection)             │
│  GPIO22  ← DHT22 Sensor 2 (Humidity/Temp)               │
│  GPIO6   ← IR Sensor 1 (Bin area detection)             │
│  GPIO5   ← IR Sensor 2 (Bin area detection)             │
│  GPIO26  ← TEC Peltier Relay (Cooling control)          │
│  GPIO27  ← DHT22 Sensor 1 (Humidity/Temp)               │
│                                                           │
│  GPIO5   ← Coin Hopper Motor 1 (Change dispenser)       │
│  GPIO6   ← Coin Hopper Motor 2 (Change dispenser)       │
│                                                           │
│  SPI/Serial ← ESP32 (Motor control via TCP)             │
│                                                           │
└──────────────────────────────────────────────────────────┘
```

## State Transitions

### TEC Controller States
```
START
  ↓
[Disabled] ← config: enabled=false
  ↓
[Initializing] ← Start monitoring thread
  ↓
[OFF] ← Temp < target + hysteresis
  ↓ (if temp > target + hysteresis)
[ON] ← GPIO26=HIGH, TEC actively cooling
  ↓
[ON] ← Maintains: target - hysteresis ≤ temp ≤ target + hysteresis
  ↓ (if temp < target - hysteresis)
[OFF] ← GPIO26=LOW, TEC stops cooling
  ↓
[OFF] → Repeat cycle
  ↓
[Shutdown] ← Cleanup GPIO on exit
```

### IR Sensor States (Per Dispense)
```
IDLE
  ↓
[DISPENSING] ← Motor activated, item falls
  ↓
[DETECTING] ← Monitoring IR sensors
  ↓
  ├─→ [SUCCESS] ← Item detected in bin (sensor status: absence)
  │
  └─→ [TIMEOUT] ← Exceeded max wait time without detection
      ↓
      → Alert user, request intervention
```

## Callback Chain Example

```
Scenario: Item dispensing with 10-second timeout

T=0.0s
  └─ User purchases item
  └─ Payment accepted
  └─ Motor activated
  └─ dispense_monitor.start_dispense(slot_1, timeout=10s)

T=0.5s - 1.0s
  ├─ Item falls through dispense area
  ├─ IR sensor detects passage
  └─ _check_item_detected() → True

T=1.0s
  ├─ _on_item_dispensed(slot_1, success=True) [CALLBACK]
  ├─ _on_ir_status_update(s1=False, s2=True, ...) [CALLBACK]
  ├─ main.py receives callbacks
  ├─ status_panel.update_ir_status(...)
  └─ Status display updates: "📡 S1: EMPTY"

Result: SUCCESS ✓
```

## System Monitor Loop

```
start_update_loop():
    while True:
        t = time.time()
        
        # Get current state (thread-safe with locks)
        dht22_data ← Locked copy
        tec_status ← Locked copy
        ir_status ← Locked copy
        
        # Update display labels
        refresh_display():
            update_dht22_display()
            update_tec_display()
            update_ir_display()
            update_health_display()
        
        # UI updates (main thread safe via tkinter)
        self.dht22_s1_label.config(text=...)
        self.tec_status_label.config(text=...)
        self.ir_s1_label.config(text=...)
        
        sleep(1.0)  # Next update in 1 second
```

---

This architecture ensures:
- **Real-time monitoring** without blocking the UI
- **Thread-safe data** with minimal locking overhead
- **Responsive user interface** with background threads
- **Clean integration** between hardware and UI layers
- **Reliable status display** with error handling
